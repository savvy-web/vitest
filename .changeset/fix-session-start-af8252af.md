---
"@savvy-web/vitest": patch
---

## Bug Fixes

Fix SessionStart hook JSON validation by adding missing hookEventName field, consuming stdin to prevent broken pipe errors, and converting additionalContext from markdown to XML tags
